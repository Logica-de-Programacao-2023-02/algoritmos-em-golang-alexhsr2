packgage main
import "fmt"
func main() {
  var soma, quantidade, n int
  x = -1
  for n != 0 {
    fmt.Print( Digite um núero: ")
    fmt.Scan(&n)

    soma += x
   quantidade++
}
média := soma/quantidade
fmt.Println("A média é", media)

}
