packgage main
import "fmt"
func main() {
  var soma, quantidade, n int
	n = -1
	for n != 0 {
		fmt.Print(" Informe um número: ")
		fmt.Scan(&n)

		soma += n
		quantidade++
	}
	média := soma / quantidade
	fmt.Println("A média é: ", média)

}
